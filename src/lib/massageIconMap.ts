import { Hand, Layers, Droplets, Flame, Move, Sparkles, type LucideIcon } from "lucide-react";

export const MASSAGE_ICON_MAP: Record<string, LucideIcon> = {
  Hand,
  Layers,
  Droplets,
  Flame,
  Move,
  Sparkles,
};

export const DEFAULT_MASSAGE_ICON = Hand;

